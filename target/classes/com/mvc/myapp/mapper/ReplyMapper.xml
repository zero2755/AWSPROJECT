<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mvc.myapp.mapper.ReplyMapper">	
	
	<insert id="createReply">
		
		insert into tbl_reply7 (rno, boardNum, reply, replyer)
		values  ( seq_reply7.nextval, #{boardNum}, #{reply}, #{replyer})
	
	</insert>
	
	<select id="readReply" resultType="com.mvc.myapp.domain.ReplyVO">
	
		select * from tbl_reply7 where rno = #{rno}
	
	</select>
	
	
	<update id="updateReply">
	
		update tbl_reply7 set reply = #{reply}, updateDate=sysdate where rno=#{rno}
	
	</update>
	
	
	<delete id="deleteReply">
		
		delete from tbl_reply7 where rno=#{rno}
		
	</delete>
	
	<select id="getReplyListWithPaging" resultType="com.mvc.myapp.domain.ReplyVO">
		 

  <![CDATA[
 select  rno, boardNum, reply, replyer, replydate, updatedate
 from 
   (
    select /*+INDEX(tbl_reply7 idx_reply7) */ 
      rownum rn,  rno, boardNum, reply, replyer, replyDate, updatedate
    from tbl_reply7
    where boardNum =  #{boardNum}
    and rno > 0
    and rownum <= #{cri.pageNum} * #{cri.amountPerPage}
   ) where rn > (#{cri.pageNum} -1) * #{cri.amountPerPage}
]]>

	</select>


	<select id="getCountByboardNum" resultType="int">
<![CDATA[
select count(rno) from tbl_reply7 where boardNum = #{boardNum}
]]>
	</select>

	<update id="updateReplyCnt">
		update tbl_board7 set replycnt = replycnt + #{amountPerPage} where boardNum = #{boardNum}
	</update>

	
	
</mapper>